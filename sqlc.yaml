version: 2
sql:
  - engine: "sqlite"
    schema: "database/schema.sql"
    queries: "database/queries"
    gen:
      go:
        out: "gen/db"
        emit_db_tags: true
        emit_json_tags: true
        json_tags_case_style: "camel"
        emit_interface: true